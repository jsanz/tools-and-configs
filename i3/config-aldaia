# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose an xft font, such as:
#font xft:DejaVu Sans Mono 10
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:DejaVu Sans Mono 12
#font pango:SourceCodePro Sans Regular 10
#font pango:Monospace 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Default layout
workspace_layout tabbed

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# start dmenu (a program launcher)
# bindsym $mod+Shift+u exec i3-dmenu-desktop
bindsym $mod+u exec --no-startup-id "rofi -show run -font \\"DejaVu 20\\" -run-shell-command '{terminal} -e \\" {cmd}; read -n 1 -s\\"'"
bindsym $mod+Shift+u exec dmenu_run

# change focus
bindsym $mod+r focus left
bindsym $mod+t focus down
bindsym $mod+n focus up
bindsym $mod+s focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+r move left
bindsym $mod+Shift+t move down
bindsym $mod+Shift+n move up
bindsym $mod+Shift+s move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+l split h

# split in vertical orientation
bindsym $mod+h split v

# enter fullscreen mode for the focused container
#bindsym $mod+i fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+o layout stacking
bindsym $mod+comma layout tabbed
bindsym $mod+ntilde layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 exec "i3-msg workspace 1; i3-msg scratchpad show"
bindsym $mod+2 exec "i3-msg workspace 2; i3-msg scratchpad show"
bindsym $mod+3 exec "i3-msg workspace 3; i3-msg scratchpad show"
bindsym $mod+4 exec "i3-msg workspace 4; i3-msg scratchpad show"
bindsym $mod+5 exec "i3-msg workspace 5; i3-msg scratchpad show"
bindsym $mod+6 exec "i3-msg workspace 6; i3-msg scratchpad show"
bindsym $mod+7 exec "i3-msg workspace 7; i3-msg scratchpad show"
bindsym $mod+8 exec "i3-msg workspace 8; i3-msg scratchpad show"
bindsym $mod+9 exec "i3-msg workspace 9; i3-msg scratchpad show"
bindsym $mod+0 exec "i3-msg workspace 10; i3-msg scratchpad show"

# assign workspaces to screens
#workspace "9: im" output LVDS1
#workspace "10: mail" output LVDS1

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# move on next and previous workspaces
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next

#-cesize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym h resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym k resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+p mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#    status_command i3status --config ~/.config/i3/i3status.conf
#    #status_command /home/jsanz/bin/i3status_custom
#	position top
#    mode hide
#    hidden_state hide
#    modifier Mod1
#    bindsym --release button3 exec --no-startup-id import /tmp/latest-screenshot.png
#}

# My settings

# Volume keys
bindsym XF86AudioRaiseVolume exec amixer -q set Master 2dB+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 2dB- unmute
bindsym XF86AudioMute exec i3-audio-toggle

# Brightness keys
bindsym XF86MonBrightnessUp exec sudo /home/jsanz/bin/brightness 50
bindsym XF86MonBrightnessDown exec sudo /home/jsanz/bin/brightness -50

bindsym $mod+Shift+h exec sudo /home/jsanz/bin/brightness 50
bindsym $mod+Shift+l exec sudo /home/jsanz/bin/brightness -50

# Calculator and sound mixer
bindsym XF86Calculator exec gnome-calculator
bindsym XF86Mail exec gnome-alsamixer

# Sound volume
bindsym $mod+Shift+w exec amixer -D pulse sset Master 5%+
bindsym $mod+Shift+v exec amixer -D pulse sset Master 5%-

# start a file manager
#bindsym $mod+z exec pcmanfm
bindsym $mod+Shift+z exec nautilus --no-desktop /home/jsanz/desktop

#bindsym $mod+z exec xfe
bindsym $mod+z exec xfe

# start code
bindsym $mod+e exec code

# start flameshot
bindsym Print exec flameshot gui 

# Show a simple calendar
bindsym $mod+Shift+c exec zenity --calendar

# Start a browser
bindsym $mod+g exec brave-browser --force-dark-mode  --profile-directory="Default"
bindsym $mod+Shift+g exec brave-browser --profile-directory="Default" --incognito

#bindsym $mod+g exec firefox -P carto 
#bindsym $mod+Shift+g exec firefox -P carto --private-window

bindsym $mod+f exec brave-browser --force-dark-mode  --profile-directory="Profile 1"
bindsym $mod+Shift+f exec brave-browser --profile-directory="Profile 1" --incognito

#bindsym $mod+f exec firefox -P default
#bindsym $mod+Shift+f exec firefox -P default --private-window

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# kill focused window
bindsym $mod+Shift+period kill
# Close session
bindsym $mod+Shift+ntilde exec /home/jsanz/bin/i3-power exit
# supend and lock
bindsym $mod+Shift+p exec /home/jsanz/bin/i3-power suspend
# Shutdown the computer (user has NOPASSWD set up at sudo config for this command)
bindsym $mod+Shift+y exec /home/jsanz/bin/i3-power shutdown

# Lock screen
bindsym $mod+Control+l exec /home/jsanz/bin/fancy-i3lock -c 000000

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+q restart
# reload the configuration file
bindsym $mod+Shift+j reload

# assign workspaces to displays

# app settings
#default_border none
#default_floating_border none
new_window 1pixel
new_float 1pixel

# Make all google-chrome use a 1-pixel border:
for_window [class="*"] border 1pixel
for_window [class="Google-chrome"] border 1pixel
for_window [class="Firefox"] border 1pixel
for_window [class="Xfce4-notifyd"] border 1pixel

# Floating windows
for_window [class="Rhythmbox"] floating enable
for_window [class="Empathy"] floating enable
for_window [class="File-roller"] floating enable
for_window [class="Nautilus" instance="file_progress"] floating enable
for_window [class="Hamster-indicator"] floating enable
for_window [class="Gnome-alsamixer"] floating enable
for_window [class="Gcalctool"] floating enable
for_window [class="Audacious"] floating enable
for_window [class="gnome-keyring-prompt-3"] floating enable
for_window [class="Gtk-recordmydesktop"] floating enable
for_window [class="Alarm-clock-applet"] floating enable
for_window [class="Xfce4-notifyd"] floating enable
for_window [class="Xfce4-appfinder"] floating enable
for_window [class="Xfce4-taskmanager"] floating enable
for_window [class="Nautilus"] floating enable
for_window [class="Thunar"] floating enable
for_window [class="Gedit"] floating enable
for_window [class="Keepassx"] floating enable
for_window [class="Yad"] floating enable
for_window [class="Gnome-calculator"] floating enable
for_window [class="com.gitlab.bitseater.meteo"] floating enable
for_window [class="zoom"] floating enable

# Assign apps to workspaces
#assign [class="Skype"] 10
#assign [class="Franz"] 1
#assign [class="Slack"] 1
#assign [class="spotify"] 2
#assign [class="Spotify"] 2

# COLORS

# class                 border  backgr. text    indicator child_border
client.focused          #2E3440 #2E3440 #ffffff #2e9ef4   #4C566A 
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #3B4252 #3B4252 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff


# -- AUTO STARTS ---
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec unclutter

# Indicators
exec nm-applet
exec indicator-sound-switcher
exec gnome-keyring-daemon --start --components=pkcs11

# Set speed trackball
exec xinput --set-prop "Logitech MX Ergo" 268 0.30

# Battery popup
exec --no-startup-id /home/jsanz/bin/i3-battery-popup 

# Delayed starts
exec sleep 10; /home/jsanz/bin/screens-select

# 30 seconds
exec sleep 30; autokey
exec --no-startup-id sleep 30; xsetroot -solid "#2e3440"

# 2 minutes
#exec --no-startup-id sleep 115; nitrogen --restore
#exec sleep 120; run_keybase

exec sleep 130; /home/jsanz/bin/backup-mail.sh
exec sleep 130; /home/jsanz/bin/delete-tweets >> ~/bin/delete-tweets.log
exec sleep 150; spotify

# exec sleep 240; synology-drive
exec sleep 240; conky
#exec sleep 180; stretchly

