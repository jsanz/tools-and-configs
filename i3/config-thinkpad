# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose an xft font, such as:
#font xft:DejaVu Sans Mono 10
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
font pango:Noto Sans 9

#font pango:SourceCodePro Sans Regular 10
#font pango:Monospace 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# Default layout
# workspace_layout tabbed
workspace_layout default

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec alacritty

# switch between windows
bindsym $mod+Shift+Return exec rofi-menu -w

# start dmenu (a program launcher)
# bindsym $mod+Shift+u exec i3-dmenu-desktop
bindsym $mod+u exec --no-startup-id "rofi -show run -font \\"DejaVu 20\\" -run-shell-command '{terminal} -e \\" {cmd}; read -n 1 -\\"'"
bindsym $mod+Shift+u exec rofi-menu -r

# change focus
bindsym $mod+h focus left
bindsym $mod+t focus down
bindsym $mod+n focus up
bindsym $mod+s focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+t move down
bindsym $mod+Shift+n move up
bindsym $mod+Shift+s move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+l split h

# split in vertical orientation
bindsym $mod+Shift+l split v

# enter fullscreen mode for the focused container
bindsym $mod+semicolon fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+o layout stacking
bindsym $mod+comma layout tabbed
#bindsym $mod+ntilde layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace
bindsym $mod+1 exec "i3-msg workspace 1"
bindsym $mod+2 exec "i3-msg workspace 2"
bindsym $mod+3 exec "i3-msg workspace 3"
bindsym $mod+4 exec "i3-msg workspace 4"
bindsym $mod+5 exec "i3-msg workspace 5"
bindsym $mod+6 exec "i3-msg workspace 6"
bindsym $mod+7 exec "i3-msg workspace 7"
bindsym $mod+8 exec "i3-msg workspace 8"
bindsym $mod+9 exec "i3-msg workspace 9"
bindsym $mod+0 exec "i3-msg workspace 10"

# assign workspaces to screens
#workspace "9: im" output LVDS1
#workspace "10: mail" output LVDS1

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10

# move on next and previous workspaces
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next

#-cesize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym h resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym k resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+p mode "resize"

# My settings

# Change layouts
bindsym $mod+Control 1 exec setxkbmap -layout us -variant dvorak-intl
bindsym $mod+Control 2 exec setxkbmap -layout us -variant intl 

# Volume keys
bindsym XF86AudioRaiseVolume exec amixer -q set Master 2dB+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 2dB- unmute
# bindsym XF86AudioMute exec i3-audio-toggle

# Brightness keys
bindsym XF86MonBrightnessUp exec sudo /home/j/bin/brightness 20
bindsym XF86MonBrightnessDown exec sudo /home/j/bin/brightness -20

#bindsym $mod+Shift+h exec sudo /home/j/bin/brightness 50
#bindsym $mod+Shift+l exec sudo /home/j/bin/brightness -50

# Calculator and sound mixer
bindsym XF86Calculator exec gnome-calculator
bindsym XF86Mail exec peek
bindsym XF86Explorer exec nautilus
bindsym XF86Favorites exec code
bindsym XF86HomePage exec dmenu-uni emoji
bindsym $mod+Home exec dmenu-uni emoji

# Sound volume
bindsym $mod+Shift+w exec amixer -D pulse sset Master 5%+
bindsym $mod+Shift+v exec amixer -D pulse sset Master 5%-

# start a file manager
#bindsym $mod+z exec pcmanfm
#bindsym $mod+z exec xfe
bindsym $mod+z exec xfe
bindsym $mod+Shift+z exec nautilus

# start code
bindsym $mod+e exec code

# start portfolioPerformance
bindsym $mod+Shift+p exec portfolioPerformance

# start flameshot
bindsym Print exec flameshot gui 

# Show a simple calendar
bindsym $mod+Shift+c exec zenity --calendar

# Start a browser
bindsym $mod+g exec brave-browser --force-dark-mode  --profile-directory="Default"
bindsym $mod+Shift+g exec brave-browser --profile-directory="Default" --incognito 

#bindsym $mod+g exec firefox -P carto 
#bindsym $mod+Shift+g exec firefox -P carto --private-window

bindsym $mod+f exec brave-browser --force-dark-mode  --profile-directory="Profile 1"
bindsym $mod+Shift+f exec brave-browser --profile-directory="Profile 1" --incognito

#bindsym $mod+f exec firefox -P default
#bindsym $mod+Shift+f exec firefox -P default --private-window

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# kill focused window
bindsym $mod+Shift+comma kill

# Close session
#bindsym $mod+Shift+period exec /home/j/bin/i3-power exit

# supend and lock
#bindsym $mod+Shift+p exec /home/j/bin/i3-power suspend

# Lock screen
#bindsym $mod+Control+l exec xautolock -locknow
bindsym $mod+Control+l exec fancy-i3lock

# Shutdown the computer (user has NOPASSWD set up at sudo config for this command)
#bindsym $mod+Shift+y exec /home/j/bin/i3-power shutdown

# System menu
bindsym $mod+Shift+y exec rofi-menu -l


# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+q restart
# reload the configuration file
bindsym $mod+Shift+j reload

# clipboard
bindsym $mod+x exec rofi-menu -c
# browser search
bindsym $mod+b exec rofi-menu -b

# assign workspaces to displays

# app settings
#default_border none
#default_floating_border none
new_window 1pixel
new_float 1pixel

# Make all google-chrome use a 1-pixel border:
for_window [class="*"] border 1pixel
for_window [class="Google-chrome"] border 1pixel
for_window [class="Firefox"] border 1pixel
for_window [class="Xfce4-notifyd"] border 1pixel

# Floating windows
for_window [class="Rhythmbox"] floating enable
for_window [class="Empathy"] floating enable
for_window [class="File-roller"] floating enable
for_window [class="Nautilus" instance="file_progress"] floating enable
for_window [class="Hamster-indicator"] floating enable
for_window [class="Gnome-alsamixer"] floating enable
for_window [class="Gcalctool"] floating enable
for_window [class="Audacious"] floating enable
for_window [class="gnome-keyring-prompt-3"] floating enable
for_window [class="Gtk-recordmydesktop"] floating enable
for_window [class="Alarm-clock-applet"] floating enable
for_window [class="Xfce4-notifyd"] floating enable
for_window [class="Xfce4-appfinder"] floating enable
for_window [class="Xfce4-taskmanager"] floating enable
for_window [class="Nautilus"] floating enable
for_window [class="Thunar"] floating enable
for_window [class="Gedit"] floating enable
for_window [class="Keepassx"] floating enable
for_window [class="Yad"] floating enable
for_window [class="Gnome-calculator"] floating enable
for_window [class="com.gitlab.bitseater.meteo"] floating enable
for_window [class="zoom"] floating enable
for_window [class="qasmixer"] floating enable
for_window [class="Arandr"] floating enable
for_window [class="ColorPicker"] floating enable
for_window [class="gnome-system-monitor"] floating enable
for_window [instance="i3-quaketerm"] border none floating enable

# Assign apps to workspaces
#assign [class="Skype"] 10

# COLORS

# class                 border  backgr. text    indicator child_border
client.focused          #2E3440 #2E3440 #ffffff #2e9ef4   #4C566A 
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #3B4252 #3B4252 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

client.background       #ffffff


# -- AUTO STARTS ---
exec_always --no-startup-id compton
exec_always --no-startup-id xset -dpms; xset s off
exec_always --no-startup-id unclutter
exec_always --no-startup-id gnome-keyring-daemon --start --components=pkcs11
exec_always --no-startup-id input --set-prop "Logitech MX Ergo" 268 0.30
exec_always --no-startup-id xss-lock -- /home/j/bin/fancy-i3lock
#exec_always --no-startup-id xautolock -time 30 -locker "/home/j/bin/fancy-i3lock" -corners "00-+" -notify 20 -notifier "notify-send -u critical 'ey!' 'Locking...'" -killtime 10 -killer "systemctl suspend"


# Battery popup
# TODO exec --no-startup-id /home/j/bin/i3-battery-popup 

# Delayed starts
exec_always --no-startup-id sleep 5; greenclip daemon 
exec_always --no-startup-id sleep 5; xsetroot -solid "#2e3440"
exec_always --no-startup-id sleep 5; sh ~/.fehbg
exec sleep 5; polybar laptop -r

#exec sleep 7; /home/j/bin/screens-select

# Indicators
exec sleep 20; nm-applet; 
exec sleep 20; indicator-sound-switcher;
exec sleep 20; blueman-applet

# 30 seconds
exec sleep 30; rescuetime
exec sleep 30; autokey
exec sleep 30; breaktimer 
exec sleep 30; conky-desktop

# TODO exec sleep 130; /home/j/bin/backup-mail.sh
# exec sleep 130; source /home/j/.pam_environment; /home/j/bin/delete-tweets >> ~/bin/delete-tweets.log

exec sleep 240; synology-drive
exec sleep 240; /opt/urserver
